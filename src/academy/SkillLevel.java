package academy;

public enum SkillLevel { // Enum to represent the skill level of a spell
    BASICO,
    INTERMEDIO,
    AVANZADO,
    ISTARI // The highest level, representing the director of the academy. It cannot be assigned to a student.
}


